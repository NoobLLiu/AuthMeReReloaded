name: Java CI

on:
  push:
  pull_request:
    branches:
      - master

jobs:
  build:
    strategy:
      matrix:
        jdkversion: [ 21 ]
    runs-on: ubuntu-latest
    outputs:
        artifact_id: ${{ steps.upload.outputs.artifact-id }}
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-java@v5
        with:
          distribution: 'zulu'
          java-version: ${{ matrix.jdkversion }}
          cache: 'maven'
      - name: Build AuthMe
        run: mvn -V -B clean package --file pom.xml
      - name: Build Geyser Extension
        run: mvn -V -B clean package --file authme-geyser-extension/pom.xml
      - name: Upload AuthMe Artifact
        uses: actions/upload-artifact@v7
        id: upload
        with:
          name: AuthMe
          path: target/AuthMe-*-FORK-Universal.jar
          archive: false
      - name: Upload Geyser Extension Artifact
        uses: actions/upload-artifact@v7
        with:
          name: AuthMe-Geyser-Extension
          path: authme-geyser-extension/target/authme-geyser-extension-*.jar
          archive: false
  runtime-test:
    name: Plugin Runtime Test
    needs: [build]
    runs-on: ubuntu-latest
    strategy:
      matrix:
        include:
          - mcVersion: '1.8.8'
            javaVersion: '8'
          - mcVersion: '1.12.2'
            javaVersion: '8'
          - mcVersion: '1.18.2'
            javaVersion: '17'
          - mcVersion: '1.20.4'
            javaVersion: '21'
          - mcVersion: '1.21.1'
            javaVersion: '21'
    steps:
      - uses: HaHaWTH/minecraft-plugin-runtime-test@paper
        with:
          server-version: ${{ matrix.mcVersion }}
          java-version: ${{ matrix.javaVersion }}
          artifact-ids: ${{needs.build.outputs.artifact_id}}
